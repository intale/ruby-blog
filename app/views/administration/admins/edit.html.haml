.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "#{t("web-app-theme.list", :default => "List")}", administration_admins_path
      %li= link_to "#{t("web-app-theme.new", :default => "New")}", new_administration_admin_path
      %li.active= link_to "#{t("web-app-theme.edit", :default => "Edit")}", edit_administration_admin_path
  .content
    %h2.title
      = t("web-app-theme.edit", :default => "Edit")
      Admin
    .inner
      = form_for @admin, :url => administration_admin_path(@admin), :html => { :multipart => true }, :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}

