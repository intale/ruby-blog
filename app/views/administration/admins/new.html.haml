.block
  .secondary-navigation
    %ul.wat-cf
      %li.first= link_to "#{t("web-app-theme.list", :default => "List")}", administration_admins_path
      %li.active= link_to "#{t("web-app-theme.new", :default => "New")}", new_administration_admin_path
  .content
    %h2.title
      = t("web-app-theme.new", :default => "New")
      Admin
    .inner
      = form_for :admin, :url => administration_admins_path, :html => { :class => :form } do |f|
        = render :partial => "form", :locals => {:f => f}
- content_for :sidebar, render(:partial => 'sidebar')
