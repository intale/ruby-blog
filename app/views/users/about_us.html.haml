.center
  - @admins.each do |admin|
    .about
      %table{:width=>"100%", :cellspacing=>"0", :cellpadding=>"0", :border=>"1"}
        %tr
          %th{:rowspan => "2",:width=>"20%"}= image_tag admin.avatar.photo.url(:medium)
          %th="Administrator name:"
          %th="Administrator email:"
        %tr
          %td{:width=>"30%"}= admin.username.split(" ").each {|word| word.capitalize!}.join(" ")
          %td{:width=>"50%"}= mail_to "#{admin.email}", "#{admin.email}", :encode => "javascript"
  .clear
