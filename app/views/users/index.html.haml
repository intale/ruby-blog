.center
  - @posts.each do |post|
    .item
      %h2
        =link_to post.subject, "#"
      .by
        ="Posted by #{post.admin.nick}@#{post.created_at}"
      =post.message.first(100)
      .more
        =link_to "Read More", "#"
  .pager
    =will_paginate @posts, :class => "pager", :previous_label => '', :next_label => '', :separator => ' - '
    .clear
  .clear
